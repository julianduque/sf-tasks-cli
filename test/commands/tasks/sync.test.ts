import {expect, test} from '@oclif/test'

describe('tasks/sync', () => {
  describe('tasks/sync', () => {
    test
    .skip()
    .stdout()
    .command(['tasks/sync'])
    .it('runs sync', _ctx => {
      expect(true).to.equal(true)
    })
  })
})
