import {expect, test} from '@oclif/test'

describe('tasks/new', () => {
  describe('tasks/new', () => {
    test
    .skip()
    .stdout()
    .command(['tasks/new'])
    .it('runs new', _ctx => {
      expect(true).to.equal(true)
    })
  })
})
